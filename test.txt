
https://github.com/jfomalhaut/wj_study.git

공유하고 있는 프로젝트의 URL

0. 터미널 사용법 기초
$ dir 
해당 폴더에 어떤 파일이나 폴더가 있는지 알려주는 역할
$ cd [폴더명]
해당 폴더로 들어가는 명령어

$ cd ..
폴더에서 뒤로 한 번 가기.


1. clone 복제.
$ git clone https://github.com/jfomalhaut/wj_study.git

2. 받는 방법 (pull)
$ git pull

3. 업로드 (push)
$ git add .
$ git commit -m "message"
$ git push

내용 추가

HTML => CSS => Javascript(기능) => React